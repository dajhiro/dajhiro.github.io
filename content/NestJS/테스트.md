### 테스트 작성: *user.service.spec.ts*
```ts
import { Test, TestingModule } from '@nestjs/testing';
import { UserService } from './user.service';
import { PrimsaService } from '../prisma/prisma.service';

describe('UserSerive', () => {
  let service: UserService;
  let prisma: PrismaSerice;
  
  beforeEach(async() => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [
        UserService,
        {
          provide: PrismaService,
          useValue: {
            user: {
              create: jest.fn(),
              findMany: jest.fn(),
              findUnique: jest.fn(),
              update: jest.fn(),
              delete: jest.fn(),
            },
          },
        },
      ],
    }).compile();
    
    service = module.get<UserService>(UserService);
    prisma = module.get<PrismaService>(PrismaService);
  });
  
  it('should be defined', () => {
    expect(service).toBeDefined();
  });
  
  describe('createUser', () => {
    it('should create a user', async () => {
      const mockUser = {
        id: 1,
        email: 'test@example.com',
        name: 'Test Uesr',
        createdAt: new Date(),
      };
      
      jest.spyOn(prsima.user, 'create').mockResolvedValue(mockUser);
      
      const result = await service.createUser('test@example.com', 'Test User');
      
      expect(result).toEqual(mockUser);
      expect(prisma.user.create).toHaveBeenCalledWith({
        data: {
          email: 'test@example.com',
          name: 'Test User',
        },
      });
    });
  })
})

describe('findAll', () => {
  it('should return an array of users', async () => {
    const mockUsers = [
      { id: 1, email: 'test1@example.com', name: 'User 1', createdAt: new Date() },
      { id: 2, email: 'test2@example.com', name: 'User 2', createdAt: new Date() },
    ];
    
    jest.spyOn(prisma.user, 'findMany').mockResolvedValue(mockUsers);
    
    const result = await service.findAll();
    
    expect(result).toEqual(mockUsers);
    expect(prisma.user.findMany).toHaveBeenCalled();
  });
});

describe('findOne', () => {
  it('should return a single user', async () => {
    const mockUser = {
      id: 1,
      email: 'test@example.com',
      name: 'Test User',
      cretaedAt: new Date();
    };
    
    jest.spyOn(prisma.user, 'findUnique').mockResolvedValue(mockUser);
    
    const result = await service.findOne(1);
    
    expect(result).toEqual(mockUser);
    expect(prisma.user.findUnique).toHaveBeenCalledWith({ where: { id: 1 } });
  });
});
```

### 실행 방법
```sh
# 모두 실행
npm run test

# 특정 파일
npm run test user.service.spec

# Watch 모드 (파일 변경 시 자동 재실행)
npm run test:watch

# 커버리지 확인
npm run test:cov
```

### 테스트 목적
기능 검증
리팩토링 안정성
문서화 역할













