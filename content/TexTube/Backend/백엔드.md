### 구조
- [[content-pipeline]]

### 로깅: 모니터링 기능을 사용하려면 (슬랙 알림, 이메일 등)
서버: Railway
비즈니스 로직 에러(API 호출 실패 등)
[[Sentry]]: 에러 추적 도구



로그 저장
- **Railway 로드 스트리밍:** Railway의 `Settings > Log Streaming` 메뉴에서 외부 서비스 주소만 입력하면 모든 로그가 실시간으로 전달됩니다.


### [[로깅]]: 어떤 걸 로깅할까?

log 계층은?
- dev
    - `verbose()` 개발용
    - `debug()` 디버깅 시 필요한 정보 (변수 값 등)
- prod
    - `log()` 시스템 흐름
    - `warn()` 예외적이지만 시스템이 중단되지는 않음
    - `error()` 심각한 문제 발생

- ID 추출 함수 진입
- 추출된 ID: abcd234
- 포스트 생성 프로세스 시작
- 자막 데이터가 비어있습니다
- 포스트 생성 실패: API 응답 없음

### [[테스트]]: 직접 만들어보자
`content-pipeline.service.spec.ts`

```ts

```

### [[테스트]]: 어떻게 마련할까?
- 단위/[[Features|기능]](여러 단위) 테스트
- 엔드포인트(API) 테스트
- DB 테스트

이런 로직으로 테스트를 하고 있는데 지금까지는 Claude Code에게 맡겼어.
얘는 ts파일을 새로 생성해서 하더라고(`ts-node`)
NestJS에서 각 테스트를 어떻게 하는 게 좋은지 설명한다. 

### 테스트 환경 준비하기
`Test.createTestingModule`

AAA 패턴으로 작성한다
- Arrange: Mock을 준비하고 세팅
- Act: 실제 함수 호출
- Assert: 검증

실행하기:
- 전부: `npm run test`
- 단일: `npx jest [file]`


### 단위 테스트
- 추천: Vitest

Vitest
방식: __tests__

### 질문
- 사용하지 않는 Controller 함수는 제거하는 게 좋을까?  
- 관리자 기능은 어떻게 마련하는 게 좋을까?

### 실제로 사용하는 엔드포인트 정리
- 게시글 목록 가져오기
- 게시글 디테일 가져오기
- URL 업로드하기(게시글 작성)
