### id
- [x] 21억개 OK <= id를 int로 하기에는 너무 많아져버리면 어떡하지?
4가지 방식
```prisma
id String @id @default(cuid())
id String @id @default(uuid()) @db.Uuid
id Int @id @default(autoincrement())
id String @id @default(cuid2())
```

cuid: Prisma가 직접
uuid: PostgreSQL가 생성

=> cuid 채택

### 콘텐츠
기본
- title
- content
- summary

### 메타데이터
기본
```prisma
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt
```

### 유튜브
```
youtubeId String @unique
youtubeUrl String
videoTitle String
channelName String
thumbnail String
```

num

Post
- id
- 핵심
    - title
    - content
    - summary
- 분류
    - category
- 메타데이터
    - 조회수, 좋아요, 
- 작가
    - 

- source
    - authorName
    - authorImage
    - authorUrl

- sharedBy User @relation("SharedBy", )

- createdAt
- updatedAt

---
Author
- id
- channelId
- name
- profileImage String
- channelUrl