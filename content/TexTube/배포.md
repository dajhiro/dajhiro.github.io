로컬과 웹 환경의 차이

Cloudflare Console

- [[환경변수]] 관리
- DB 관리

### Cloudflare

textube.io를 열자마자 미친듯이 들어오는 이유가 뭐지? 공격하려고? 


## Railway
### 질문
- [x] Docker를 사용하지 않아도 되는가?
- [x] 환경변수를 알아서 찾는가? NestJS에서 환경변수를 구분하는 방법이 있었는데
    - [x] 내 서버는 ConfigService를 이용하고 있는가?
    - [x] ConfigService를 사용할 경우 Railway에서는 어떻게 구현되는가?
    - [x] [[Publish/NestJS/NestJS|NestJS]]에선 `.env`를 읽어들인다. 그러나 Railway가 우선이다.
- [ ] 도메인은 어떻게 될까? textube.io/api 로 매핑시킬수는 없나.
    - [ ] api.textube.io 로 
- [ ] 배포하고 나면 스키마를 바꾸는 것이 거의 불가능한가? 그렇다면 어떻게 해야 하는가? 일일이 입력할 수는 없는 노릇이다.
    - [ ] 삭제는 번거로울 수 있다. 그러나 간단한 이름 변경이나, 새로운 값 추가는 쉬울 수 있다.
    - [ ] 새로운 값을 추가하면 데이터들이 column이 비어버리게 되지 않나? 그럼 어떻게 해결해야 하는가?
- [ ] 현재 Railway Postgres를 사용할 건데, 나중에 마이그레이션을 잘 할 수 있을까?

### 도메인이 어떻게 될까?
https://api.textube.io

## [[CORS]]가 뭐야
### Todo
- [ ] Google Auth에서 뚫기

### 4. 구글 콘솔(Google Cloud Console) 설정 잊지 마세요! 
백엔드 주소를 `api.textube.io`로 바꿨으니, **구글 클라우드 콘솔**에 가서도 수정을 해줘야 로그인이 터지지 않습니다.
1. **승인된 JavaScript 출처:** `https://textube.io` 추가
2. **승인된 리디렉션 URI:** `https://api.textube.io/auth/google/callback` 추가

이거 왜 안돼 아오


### Cross-Origin Resource Sharing
내가 허락한 곳에서만 서버에 데이터를 요청할 수 있도록
기본값: 설정하지 않으면
같은 도메인, 같은 포트, 

### **Railway [Variables] 탭**

| **변수명 (Key)**      | **값 (Value)**                              |
| ------------------ | ------------------------------------------ |
| **`CORS_ORIGINS`** | `https://textube.io,http://localhost:3000` |
| **`NODE_ENV`**     | `production`                               |

포트 번호는 4000으로 해도 돼? 로컬 환경이랑 다르게 해도 돼? Next.js가 3000을 처먹어버려서
cloudflare에 배포한 Next.js은 8080 포트겠지? https니까
아니, 접속할 때는 443 포트다
Railway env에 PORT=3000으로 추가하면 되나

### DB
이제 지우는 건 자제해야 할 것이다.
- [ ] 새로 생성된 칼럼에는 기본값이 존재해야 한다.

migration
코드는 새 버전인데 DB는 구버전이면 에러가 나겠죠?

package.json
```diff
+    "build": "prisma generate && prisma migrate deploy && nest build",

+    "db:migrate:deploy": "prisma migrate deploy",
+    "db:migrate:dev": "prisma migrate dev",
+    "db:push": "prisma db push",
+    "db:studio": "prisma studio",
+    "db:reset": "prisma migrate reset",
+    "db:seed": "ts-node prisma/seed.ts"
```

