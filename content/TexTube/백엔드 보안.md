[[260320(금)]]
스프링을 너무 어렵게 생각하는 것 같다.
Spring과 Spring Boot의 차이는?

Spring을 알면 Spring Boot 도 쉬워지는가? 
현재 채용 시장에서 바라는 JAVA 개발자의 역량은?

- [x] [[백엔드 공부]]

NestJS가 Spring에 비해 우위를 가지는 영역은?
- I/O Bound
    - 채팅, 알림, 단순 API 가교
- 반대: CPU Bound
    - 수학적 계산이나 대용량 데이터 가공
    - JVM의 JIT 컴파일러가 코드를 기계어로 최적화

금융 도메인, 복잡한 알고리즘이 포함된 서비스는
Spring Boot


## 260319(목)
```
TexTube Backend 보안 및 구조 분석 결과

전체 소스코드(컨트롤러 7개, 서비스 13개, 모듈 11개, DTO 10개 등)를 분석했습니다.

---
🔴 CRITICAL (즉시 수정 필요)

1. POST/PATCH/DELETE 엔드포인트에 인증 없음

파일: src/posts/posts.controller.ts, src/upload/upload.controller.ts

@UseGuards(SessionAuthGuard) 데코레이터가 없어서 누구든 인증 없이 글 생성/수정/삭제 가능.

# 인증 없이 그냥 호출 됨
curl -X POST http://localhost:4000/posts -d '{"title":"hack","youtubeId":"..."}'

2. 개발용 엔드포인트가 인증 없이 노출

파일: src/app.module.ts, src/dev/dev.controller.ts

const isDevelopment = process.env.NODE_ENV !== 'production'; ...(isDevelopment ? [DevModule] : []),

NODE_ENV가 잘못 설정된 경우 production에도 노출. /dev/test-youtube-analysis,
/dev/test-innertube-* 등 AI 분석 + 자막 추출 작업을 인증 없이 무제한 호출 가능 → 비용 폭발.

---
🟠 HIGH (빠른 수정 필요)

3. 세션 시크릿 하드코딩 폴백

파일: src/main.ts:47

secret: configService.get('SESSION_SECRET') || 'fallback-secret',

SESSION_SECRET 미설정 시 소스코드에 있는 'fallback-secret' 사용 → 세션 위조 가능.

4. sameSite: 'none' CSRF 취약점

파일: src/main.ts:54

sameSite: isProduction ? 'none' : 'lax',
// domain: isProduction ? '.textube.io' : undefined,  // 주석처리됨

Production에서 sameSite: 'none'은 CSRF 공격에 취약. domain도 주석 처리됨.

---
🟡 MEDIUM (계획적 수정)

5. CORS - Origin 없는 요청 전체 허용

파일: src/main.ts:29

if (!origin) return callback(null, true);  // curl, Postman 등 다 허용

6. 리소스 소유권 검증 없음

파일: src/posts/posts.service.ts

update(), remove() 메서드에서 해당 post가 요청한 유저의 것인지 확인하지 않음 → 다른
유저의 글 수정/삭제 가능.

7. Rate Limiting 없음

/upload (AI 분석 포함), /dev/* 등 비용이 큰 엔드포인트에 요청 제한 없음 → DoS 및 비용
폭발.

8. YouTube API Key URL 파라미터에 노출

파일: src/integrations/youtube-data/youtube-data.service.ts:23,40

`${baseUrl}/videos?part=snippet&id=${videoId}&key=${this.apiKey}`

API Key가 URL에 포함되어 서버/프록시 로그에 기록될 수 있음.

9. 외부 API 요청 타임아웃 없음

innertube-legacy.service.ts, youtube-data.service.ts에서 fetch() 사용 시 타임아웃
미설정 → 서버 hang 가능.

10. DTO 타입 안전성 부족

metadata?: any;       // CreatePostDto
posts: any[];         // GetPostsResponseDto

---
🟢 긍정적 발견

┌───────────────────┬─────────────────────────────┐
│       항목        │            상태             │
├───────────────────┼─────────────────────────────┤
│ SQL Injection     │ ✅ Prisma ORM 사용으로 방지 │
├───────────────────┼─────────────────────────────┤
│ 비밀번호 노출     │ ✅ OAuth only               │
├───────────────────┼─────────────────────────────┤
│ httpOnly 쿠키     │ ✅ 적용됨                   │
├───────────────────┼─────────────────────────────┤
│ Secure 쿠키       │ ✅ Production 적용          │
├───────────────────┼─────────────────────────────┤
│ TypeScript strict │ ✅ 활성화                   │
├───────────────────┼─────────────────────────────┤
│ class-validator   │ ✅ DTO 검증 적용            │
└───────────────────┴─────────────────────────────┘

---
우선순위별 수정 계획

즉시 (오늘):
1. posts.controller.ts, upload.controller.ts - @UseGuards(SessionAuthGuard) 추가
2. main.ts - SESSION_SECRET 미설정 시 앱 시작 거부 (throw new Error())
3. app.module.ts - DevModule에 인증 가드 추가 또는 아예 제거

단기 (이번 주):
4. posts.service.ts - update/remove에 userId 소유권 검증 추가
5. Rate limiting 미들웨어 (@nestjs/throttler) 도입
6. sameSite: 'strict' 또는 'lax'로 변경, domain 설정 복원

중기 (다음 스프린트):
7. CORS origin 없는 요청 차단 정책 재검토
8. 외부 API fetch에 AbortController + 타임아웃 추가
9. 에러 응답에서 내부 스택/메시지 숨기기
10. 전체 목록 API에 페이지네이션 추가
```

---
의문점



---
님아. Post랑 Upload 모듈이 나눠져있거든.
- Post는 단순 Dto고,
- Upload는 내 TexTube의 고유 기능인 링크 => 업로드 기능이야. 

4가지
- GET - Read
- POST - Create
- PATCH - Update
- DELETE - Delete

[[DBMS]]
Supabase

Upload는 Post의 PATCH와 POST만

Post
### POST
- [x] GET:
- [x] POST: 기존 Upload 기능
- [ ] PATCH: Upload 덮어쓰기 특정 조건에서
    - [ ] 조건: 금액을 내고 새로운 게시글을 만드려고 할 때 - 업그레이드만 허용
- [ ] DELETE: Contributor



수정/삭제
삭제 권한을 웬만하면 안 두려고 하는데
예를 들어 좋아요를 누른 모두가 Contributor고
삭제를 하는 건 다른 Contributor가 존재하면 내 게시글 목록에서 삭제될 뿐이지
삭제 자체는 되지 않아
그 프로세스를 통해 Delete를 구현시키려고 해

Patch는 새로운 Update를 실행하지 않도록
일반적으로는 

---
## 1. 가드 추가하기
```ts
import {
  UseGuards,
  Req,
} from '@nestjs/common';
import type { Request } from 'express';
import { SessionAuthGUard } from '@auth/guards/session-auth.guard';
```

```ts
  @Post()
  @UseGuards(SessionAuthGuard)
  create(@Body() createPostDto: CreatePostDto, @Req() req: Request) {
    return this.postService.create({ ...createPostDto, userId: req.user!.id });
  }
  
  @Path(':id')
  @UseGuards(SessionAuthGuard)
  update(
    @Param('id', ParseIntPipe) id: number,
    @Body() updatePostDto: UpdatePostDto,
  ) {
  }

```

- `posts/upload`

`UseGuards`
`Req`























