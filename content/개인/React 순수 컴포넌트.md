[컴포넌트를 순수하게 유지하기 – React](https://ko.react.dev/learn/keeping-components-pure)
- 순수성, 그리고 버그 회피
- 렌더링 단계에서 변화를 막아 순수성 유지
- 엄격 모드(Strict Mode)를 활용해 실수 발견

**자신의 일에만 집중합니다.**
- 함수가 호출되기 전에 존재했던 어떤 객체나 변수도 변경하지 않습니다.

**같은 입력, 같은 출력.**
- 같은 입력이 주어졌다면 순수 함수는 항상 같은 결과를 반환합니다.

지역 변경

변이
Mutation

**해당 컴포넌트를 여러 번 호출할 때마다 서로 다른 JSX를 생성한다**
이것은 예측 불가능한 동작입니다.

일반적으로 컴포넌트가 특정 순서대로 렌더링될 것이라고 기대하면 안 됩니다.

함수형 프로그래밍은 순수성에 크게 의존하지만, 결국 어느 시점에 어디선가 _무언가는_ 바뀌어야 합니다.

useEffect


---
- [[순수 함수]]
- [memo – React](https://ko.react.dev/reference/react/memo)
- 