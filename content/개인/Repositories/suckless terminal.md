

내가 이걸 왜 깔았지? Vim 의 화면 출력을 이해하기 위해서
왜 이해하려고 하지? CLI 앱을 만드려고

[[CLI]]는 출력이 아쉽다.
스크롤하기 귀찮다.
원하는 걸 빠르게 찾지 못한다.
파일로 저장해도 불편하다.
왤까? 어디서부터 문제?

터미널에는 **탐색**(Navigate)이라는 개념 자체가 없다.
scrollback은 그냥 메모리에 누적된 텍스트 버퍼일 뿐

scrollback

Unix: `jq` + JSON
PowerShell: 파이프라인에 텍스트 대신 객체를 흘려보냄(pipe)

`less`나 `fzf`


Interpret Mode?


---
기타
- [[st.h]]
- [`st`](suckless terminal)
이거 suckless terminal이 아닌 suckless로 들어가져버리는데(Obsidian) 어떻게 해결??
⇒ `<>`로 감싸면 된다.

---


