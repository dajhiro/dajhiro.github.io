[[../개인/스크립트]]


---
## RegExp
두번째 매칭 가져오기
### Excel
- `REGEXTEST(text, pattern)`
- `REGEXEXTRACT(text, pattern)`
- `REGEXREPLACE(text, pattern, replacement)`

스필 배열?
배열 형태

2번은 여러 매칭이구나
`=REGEXEXTRACT(A1, "(\d+)-(\d+)-(\d+)", 2)`

 ### VBA가 필요해지는 순간
- **반복 작업 자동화** — 버튼 클릭 한 번으로 100개 시트 처리
- **외부 연동** — 파일 시스템 접근, 다른 Office 앱 제어
- **이벤트 처리** — 셀 값이 바뀔 때 자동 실행
- **커스텀 함수** — 함수로 구현 불가능한 복잡한 로직
- **UI 제어** — 사용자 폼, 팝업 다이얼로그

[[Excel]]
프로그램 설계

JavaFX

[[AutoHotKey]]
